<div>
    <h1>All Posts</h3>
    {{#each posts as |post|}}
    <div class="post">
        <h2>{{post.title}}</h2>
        <h3>{{format_date post.createdAt}}</h3>
        <h4>{{post.body}}</h4>
        <hr>
    </div>
    {{/each}}
</div>